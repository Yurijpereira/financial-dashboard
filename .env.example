# PostgreSQL (used by docker-compose; override to avoid committing real credentials)
POSTGRES_USER=findash
POSTGRES_PASSWORD=findash_dev_2026
POSTGRES_DB=findash

DATABASE_URL="postgresql://findash:findash_dev_2026@localhost:5432/findash?schema=public"
NUXT_SESSION_PASSWORD="change-me-to-a-random-string-with-at-least-32-characters"
